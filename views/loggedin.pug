extends template/layout 
block content
    container.ml-2
        .ml-2
            div(class="form-signin")
                div.text-center
                    p Hello #{username}!
    #isAdmin
        if isAdmin
            button.bg-blue-900.text-white.font-bold.py-2.px-4.rounded(class='hover:bg-blue-900' type='button')
                a(href="/") Log Out


